# StreetfoodSpotter

Next.js (App Router) + Tailwind + SQLite (better-sqlite3) app to browse, search, view, and add streetfood spots.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Notes
- The app uses a local SQLite file `spots.db` created in the project root.
